# 🚀 快速开始指南

这是一个 10 分钟快速上手教程,让你快速部署并开始使用申请博士记录系统。

## 📋 前提条件

- 拥有 GitHub 账号
- 拥有电子邮箱(用于注册 Supabase)
- 基础的网页操作能力

## ⏱️ 第一步: 设置数据库 (3 分钟)

### 1.1 注册 Supabase

1. 访问 [https://supabase.com](https://supabase.com)
2. 点击 "Start your project"
3. 使用 GitHub 账号登录(推荐)或邮箱注册

### 1.2 创建项目

1. 在 Dashboard 点击 "New Project"
2. 选择组织(Organization),首次使用会自动创建
3. 填写项目信息:
   - **Name**: `phd-applications` (可自定义)
   - **Database Password**: 设置一个强密码并保存
   - **Region**: 选择 `Singapore` (速度较快)
4. 点击 "Create new project"
5. 等待 2 分钟,项目初始化完成

### 1.3 初始化数据库

1. 在项目页面左侧菜单找到 **SQL Editor**
2. 点击 "New query"
3. 打开本地文件 `database/schema.sql`
4. 复制全部内容,粘贴到 SQL Editor
5. 点击右下角 "Run" 按钮执行
6. 看到 "Success" 提示即成功

7. 重复步骤 2-5,执行 `database/seed.sql` (添加默认模板,可选)

### 1.4 获取密钥

1. 左侧菜单点击 **Settings** → **API**
2. 找到以下信息并复制保存:
   - **Project URL**: 例如 `https://abcdefgh.supabase.co`
   - **Project API keys** → `anon` `public`: 一串很长的字符
   - **Project API keys** → `service_role`: 另一串字符(⚠️ 保密)

## 🎨 第二步: 配置前端 (1 分钟)

1. 打开本地文件 `frontend/js/supabase-config.js`
2. 替换以下内容:

```javascript
const SUPABASE_URL = 'https://abcdefgh.supabase.co'  // 粘贴你的 Project URL
const SUPABASE_ANON_KEY = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...'  // 粘贴你的 anon key
```

3. 保存文件

## ☁️ 第三步: 部署网站 (3 分钟)

### 3.1 上传到 GitHub

1. 在 GitHub 创建新仓库:
   - 访问 [https://github.com/new](https://github.com/new)
   - Repository name: `phd-application-tracker`
   - 选择 **Private** (私有,保护隐私)
   - 点击 "Create repository"

2. 在本地运行命令(在 `申请博士记录` 目录下):

```bash
git init
git add .
git commit -m "初始提交"
git branch -M main
git remote add origin https://github.com/你的用户名/phd-application-tracker.git
git push -u origin main
```

### 3.2 部署到 Vercel

1. 访问 [https://vercel.com](https://vercel.com)
2. 点击 "Continue with GitHub" 登录
3. 点击 "Add New..." → "Project"
4. 在列表中找到 `phd-application-tracker`,点击 "Import"
5. 配置:
   - **Framework Preset**: Other
   - **Root Directory**: 留空
   - **Build Command**: 留空
   - **Output Directory**: `frontend`
6. 点击 "Deploy"
7. 等待 1-2 分钟,部署完成

### 3.3 获取网址

部署完成后会显示:

```
🎉 Congratulations!
Your project has been deployed.
```

点击 "Visit" 或复制显示的网址(例如: `https://phd-application-tracker.vercel.app`)

在手机或电脑浏览器打开这个网址,系统已经可以使用了! 🎊

## 🕷️ 第四步: 配置爬虫 (3 分钟,可选)

如果你想自动爬取导师信息,继续以下步骤。否则可以跳过,手动添加导师。

### 4.1 安装 Python 环境

确保电脑上安装了 Python 3.8+:

```bash
python --version  # 或 python3 --version
```

如果没有,访问 [https://www.python.org/downloads/](https://www.python.org/downloads/) 下载安装。

### 4.2 安装依赖

```bash
cd crawler
pip install -r requirements.txt
```

### 4.3 配置环境变量

1. 复制 `.env.example` 为 `.env`:

```bash
cp .env.example .env
```

2. 编辑 `.env` 文件:

```
SUPABASE_URL=https://abcdefgh.supabase.co  # 粘贴你的 URL
SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...  # 粘贴 service_role key
```

### 4.4 测试爬虫

```bash
python main.py --dry-run
```

看到类似输出即成功:

```
🧪 测试模式
============================================================
计划爬取 1 所学校

[1/1] 北京理工大学自动化学院
------------------------------------------------------------
开始爬取: 北京理工大学自动化学院
...
✓ 找到 47 位导师
```

### 4.5 正式爬取

```bash
python main.py
```

爬取的数据会自动同步到 Supabase,刷新网页即可看到导师列表!

## ✅ 完成!

现在你可以:

1. 打开部署的网址
2. 查看导师列表
3. 点击"查看详情"查看导师信息
4. 点击"标记已发送"记录申请进度
5. 右上角切换"你"或"女朋友",协同工作

## 🎯 下一步

- 📖 阅读 [完整文档](../README.md) 了解更多功能
- 🕷️ 添加更多学校到 `crawler/config.yaml`
- 📧 自定义邮件模板
- 📊 查看统计面板分析申请进度

## ❓ 遇到问题?

### 常见问题

**Q: 网页显示"配置错误"**

A: 检查 `frontend/js/supabase-config.js` 中的 URL 和 Key 是否正确复制

**Q: 导师列表为空**

A: 运行爬虫 `python main.py`,或在网页上手动添加导师

**Q: 爬虫报错**

A: 检查 `.env` 文件配置是否正确,确保使用的是 `service_role` key

**Q: Vercel 部署失败**

A: 确保 `vercel.json` 中 `outputDirectory` 设置为 `frontend`

### 获取帮助

- 查看 [README.md](../README.md) 的常见问题章节
- 在 GitHub 提 Issue
- 检查浏览器控制台(F12)的错误信息

---

祝你申请顺利! 如果系统帮到了你,欢迎给项目点个 Star ⭐
